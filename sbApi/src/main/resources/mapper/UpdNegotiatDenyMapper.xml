<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.app.mapper.UpdNegotiatDenyMapper">
    <update id="setNegotiationStatus" parameterType="com.web.app.domain.Negotiation">
        UPDATE case_negotiations
        SET
            Status = #{status},
            LastModifiedDate = #{lastModifiedDate},
            LastModifiedBy = #{loginUser},
            User = '',
            SubmitDate = null
        WHERE
            id = #{negotiationId}
    </update>

    <select id="getNegotiationStatus" resultType="java.lang.String">
        SELECT
            Status
        FROM
            case_negotiations
        WHERE
            id = #{negotiationId}
    </select>
</mapper>