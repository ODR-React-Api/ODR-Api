<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.app.mapper.InsertFileInfoMapper">
    <insert id="insertFile" parameterType="com.web.app.domain.MedUserChange.InsertFileInfo">
        INSERT INTO files (
            id,
            CaseId,
            FileName,
            FileExtension,
            FileUrl,
            FileSize,
            RegisterUserId,
            RegisterDate
        ) VALUES (
            #{fileId},
            #{caseId},
            #{fileName},
            #{fileExtension},
            #{fileUrl},
            #{fileSize},
            #{registerUserId},
            NOW()
        )
    </insert>
    <insert id="insertCaseFileRelations" parameterType="com.web.app.domain.MedUserChange.InsertFileInfo">
        INSERT INTO case_file_relations (
            id,
            CaseId,
            RelationType,
            RelatedId,
            fileId
        ) VALUES (
            #{caseFileRelationsId},
            #{caseId},
            7,
            #{relatedId},
            #{fileId}
        )
    </insert>
</mapper>