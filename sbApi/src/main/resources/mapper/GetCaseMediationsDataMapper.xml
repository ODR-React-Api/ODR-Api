<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.app.mapper.GetCaseMediationsDataMapper">
    <!-- API_調停内容取得 -->
    <!-- 調停内容の取得 -->
    <select id="getCaseMediations" resultType="com.web.app.domain.Entity.CaseMediations">
        SELECT
            CaseId,
            Status,
            ExpectResloveTypeValue,
            PayAmount,
            CounterClaimPayment,
            PaymentEndDate,
            ShipmentPayType,
            SpecialItem,
            LastModifiedDate
        FROM case_mediations
        WHERE
            CaseId = #{caseId}
        AND Status != 0
        AND DeleteFlag = 0
    </select>

    <!-- 添付資料の取得 -->
    <select id="getFiles" resultType="com.web.app.domain.Entity.Files">
        SELECT
            files.FileName,
            files.FileUrl
        FROM case_file_relations, files
        WHERE
            case_file_relations.CaseId = #{caseId}
        AND case_file_relations.RelationType = 5
        AND case_file_relations.DeleteFlag = 0
        AND files.id = case_file_relations.fileId
        AND files.DeleteFlag = 0
        ORDER BY files.LastModifiedDate
    </select>
</mapper>
