<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.app.mapper.UserIdentity.UserIdentityMapper">
    <!--  -->
    <select id="FindUserIdentity" resultType="com.web.app.domain.UserIdentity.UserIdentity">
        SELECT
            *
        FROM
            case_relations
        WHERE
            CaseId = #{caseId}
    </select>
</mapper>