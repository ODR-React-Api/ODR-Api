<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.app.mapper.GetLoginUserRoleOpenInfoMapper">
<select id="selectLoginUserRoleOpenInfo" resultMap="com.web.app.domain.GetFileInfo">
    SELECT
        MediatorDisclosureFlag, 
        PetitionUserId,
        TraderUserEmail,
        MediatorUserEmail
    FROM
        react_use.cases t1
    LEFT JOIN
        react_use.case_relations t2
    ON
        t1.cid = t2.CaseId
    WHERE 
        t1.cid = #{caseId}
        AND t1.DeleteFlag = '0'
        AND t2.DeleteFlag = '0';
</select>
</mapper>