<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.app.mapper.UpdCasesStatusMapper">
    <select id="participatedStatusSearch" resultType="com.web.app.domain.Entity.Cases"> 
        SELECT
            cid,
            CaseStage,
            CaseStatus
        FROM cases
        WHERE
            cid = #{caseId}
            AND DeleteFlag = 0
    </select>

    <update id="caseStatusChangeUpdate"> 
        UPDATE cases
        SET CaseStatus = "0001"
        WHERE 
            cid = #{caseId} 
            AND DeleteFlag = 0
    </update>
</mapper> 